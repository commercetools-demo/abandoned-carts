fetch the configuration custom object when running the service and use the variables in that object to query for abandoned carts using the time frames specified
If I click on one of the lines in the abandoned carts list, create a pop-up window that shows me the full JSON data for that custom object
Add two additional columns to the abandoned carts table. Column labels:  'Email Sent', and 'Cart Converted'.  Fill these columns with dummy data:  use the Abandonment Date for the Email Sent column and one day after Abandonment Date for Cart Converted.
Make the table sorted by abandonment date in descending order
Change the email sent date to be one day after the abandonment date, and the cart converted date to be a random number of days between one and four after the email sent
rename directory abandoned-cart-creation-event to abandoned-cart-created-event and update any references
delete abandoned-cart-created-event and all references to it
modify mc-app:  remove service directory and instead make an HTTP call to URL specified by environment variable ABANDONED_CART_SERVICE_URL to call the abandoned cart processor service
modify the service to only fetch carts with an Active state in addition to the time parameters.  Also, when done, create a custom object with container = 'abandoned-cart' and key 'service-log'.  Put values of the last run time, the number of carts fetched, and the number of carts for which an abandoned cart object was created
modify mc-app to display the results from the service run after it completes - remove 'dummy' data
modify service/src/services/abandoned-cart.service.ts to also include the last modified date parameters in the where clause of query, as well as only fetch carts where the custom field 'abandoned' is not true.  Also, for any carts that match this, and for which we create a cart abandonment custom object, also set this custom field to true in the cart itself.  custom type:  abandoned-cart-custom and custom field: abandoned (boolean)
The Service administration page in MC app should display last run statistics by fetching a custom object with container 'abandoned-cart' and key 'service-log'.  Format for this object is in service/src/services/abandoned-cart.service.ts
