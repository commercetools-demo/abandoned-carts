query FetchServiceAdministration($container: String!, $key: String!) {
  customObject(container: $container, key: $key) {
    id
    version
    container
    key
    value
    createdAt
    lastModifiedAt
  }
}
